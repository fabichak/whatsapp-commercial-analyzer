sleep 180m

claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S3-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S3-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S4-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S4-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S4-T2 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S4-T2"


sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S5-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S5-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S6-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S6-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M3-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M3-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S6-T2 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S6-T2"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S6-T3 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S6-T3"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S7-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S7-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M2-S8-T1 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M2-S8-T1"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M3-T2 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M3-T2"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M3-T3 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M3-T3"

sleep 2m
claude --dangerously-skip-permissions -p "Speak ike a caveman. Using Context7, sequential-thinking. Read @TECH_PLAN.md and then work on M3-T4 and verify the it according to spec. Do not read PROGRESS.md."
git add .
git commit -m "M3-T4"